# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# RESEND (SMTP para Supabase Auth)
# =============================================================================
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key

# =============================================================================
# OPENROUTER API
# =============================================================================
OPENROUTER_API_KEY=or-api-key

# =============================================================================
# CLOUDFLARE R2 STORAGE
# =============================================================================
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                    MINI TUTORIAL: CONFIGURANDO O R2                        │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# 1. CRIAR BUCKET R2:
#    - Acesse: https://dash.cloudflare.com → R2 Object Storage
#    - Clique em "Create bucket"
#    - Escolha um nome (ex: "animalove-storage")
#    - Região: Automatic (recomendado)
#
# 2. OBTER ACCOUNT ID:
#    - Na dashboard do Cloudflare, veja a URL: dash.cloudflare.com/<ACCOUNT_ID>
#    - Ou vá em: Overview → Account ID (coluna direita)
#
# 3. CRIAR API TOKEN (Access Key):
#    - Vá em: R2 → Manage R2 API Tokens
#    - Clique em "Create API Token"
#    - Permissões: "Object Read & Write" para o bucket criado
#    - Copie o Access Key ID e Secret Access Key (só aparece 1 vez!)
#
# 4. (OPCIONAL) CONFIGURAR DOMÍNIO PÚBLICO:
#    - No bucket → Settings → Public Access
#    - Ative "Allow Access" ou conecte um Custom Domain
#    - A URL pública será: https://seu-dominio.com ou
#      https://pub-<hash>.r2.dev
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           VARIÁVEIS DE AMBIENTE                            │
# └─────────────────────────────────────────────────────────────────────────────┘

# Account ID da Cloudflare (encontrado na dashboard)
CLOUDFLARE_R2_ACCOUNT_ID=your-account-id

# Access Key ID do token R2 (criado em "Manage R2 API Tokens")
CLOUDFLARE_R2_ACCESS_KEY_ID=your-access-key-id

# Secret Access Key do token R2 (gerado junto com o Access Key ID)
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your-secret-access-key

# Nome do bucket R2 que você criou
CLOUDFLARE_R2_BUCKET_NAME=your-bucket-name

# (Opcional) URL pública do bucket para acessar arquivos diretamente
# Só preencha se você habilitou acesso público no bucket
# Exemplos:
#   - R2.dev subdomain: https://pub-abc123.r2.dev
#   - Custom domain: https://cdn.seudominio.com
CLOUDFLARE_R2_PUBLIC_URL=

# =============================================================================
# USO NO CÓDIGO
# =============================================================================
#
# // Importar funções do R2
# import { uploadToR2, downloadFromR2, deleteFromR2 } from "@/lib/cloudflare";
#
# // Upload de arquivo
# const result = await uploadToR2("pasta/arquivo.jpg", buffer, "image/jpeg");
#
# // Download de arquivo
# const { body, contentType } = await downloadFromR2("pasta/arquivo.jpg");
#
# // Deletar arquivo
# await deleteFromR2("pasta/arquivo.jpg");
#
# // Gerar URL assinada para upload direto do cliente
# const url = await getPresignedUploadUrl("pasta/arquivo.jpg", "image/jpeg");
#
# // API Routes disponíveis:
# // POST /api/upload - Upload via FormData
# // GET  /api/upload?filename=x&contentType=y - Gerar presigned URL
# // GET  /api/files?key=x - Download arquivo
# // GET  /api/files?prefix=pasta/ - Listar arquivos
# // DELETE /api/files?key=x - Deletar arquivo
#

# =============================================================================
# KIE.AI - UNIFIED AI GENERATION API
# =============================================================================
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                    MINI TUTORIAL: CONFIGURANDO O KIE.AI                    │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# 1. CRIAR CONTA:
#    - Acesse: https://kie.ai
#    - Crie uma conta gratuita (inclui créditos de teste)
#
# 2. OBTER API KEY:
#    - Acesse: https://kie.ai/api-key
#    - Clique em "Create API Key"
#    - Copie a chave gerada
#
# 3. ADICIONAR CRÉDITOS (se necessário):
#    - Acesse: https://kie.ai/billing
#    - Adicione créditos conforme necessário
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                         MODELOS DISPONÍVEIS                                │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# IMAGEM:
#   - nano-banana     : Google Nano Banana (text-to-image)
#   - flux2           : Flux 2 Pro (high quality text-to-image)
#   - grok-image      : xAI Grok Imagine (photorealistic)
#
# VÍDEO:
#   - veo3            : Google Veo 3.1 (text/image-to-video)
#   - sora2           : OpenAI Sora 2 (text/image-to-video)
#   - kling           : Kling 2.5 (image-to-video, optimized for couples)
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           VARIÁVEL DE AMBIENTE                             │
# └─────────────────────────────────────────────────────────────────────────────┘

# API Key do Kie.ai (obtida em https://kie.ai/api-key)
KIE_API_KEY=your-kie-api-key

# =============================================================================
# USO NO CÓDIGO
# =============================================================================
#
# // Importar funções do Kie.ai
# import {
#   generateWithNanoBanana,
#   generateWithFlux2,
#   generateWithGrokImage,
#   generateWithVeo3,
#   generateWithSora2
# } from "@/lib/kie";
#
# // Gerar imagem com Nano Banana
# const image = await generateWithNanoBanana({
#   prompt: "A futuristic cityscape",
#   aspectRatio: "16:9"
# });
#
# // Gerar imagem com Flux 2
# const image = await generateWithFlux2({
#   prompt: "Mountain landscape at sunset",
#   resolution: "2K"
# });
#
# // Gerar imagem com Grok
# const image = await generateWithGrokImage({
#   prompt: "Cyberpunk street scene",
#   aspectRatio: "16:9"
# });
#
# // Gerar vídeo com Veo 3.1
# const video = await generateWithVeo3({
#   prompt: "Cat playing piano in jazz club",
#   model: "veo3_fast",  // ou "veo3" para qualidade máxima
#   aspectRatio: "16:9"
# });
#
# // Gerar vídeo com Sora 2
# const video = await generateWithSora2({
#   prompt: "Timelapse of flowers blooming",
#   aspectRatio: "landscape",
#   nFrames: "15"
# });
#
# // Gerar vídeo com Kling 2.5 (otimizado para casais)
# const video = await generateKlingWithMultishot({
#   imageUrl: "https://...",
#   prompt: "Romantic couple portrait, tender loving moment",
#   promptType: "romantic_generic",  // romantic_gaze, romantic_embrace, etc.
#   duration: "5",                   // 5 ou 10 segundos
#   model: "v2.5-turbo-pro"         // v2.5-turbo-pro, v2.5-turbo, v2.1-master, v2.1-standard
# });
#
# // Gerar múltiplos vídeos em paralelo (batch)
# const taskIds = await batchStartKlingGeneration([
#   { imageUrl: "https://...", prompt: "First moment" },
#   { imageUrl: "https://...", prompt: "Second moment" },
#   { imageUrl: "https://...", prompt: "Third moment" }
# ], { duration: "5", model: "v2.5-turbo-pro" });
#
# // API Routes disponíveis:
# // POST /api/ai/generate - Geração unificada
# //   Body: { model: "nano-banana|flux2|grok-image|veo3|sora2",
# //           async: false, options: { prompt: "...", ... } }
# //
# // GET /api/ai/task?taskId=xxx - Status da task (para async: true)
# // GET /api/ai/task?taskId=xxx&type=veo - Status de task Veo3
# //
# // POST /api/kie/video - Gerar vídeo com Kling 2.5
# //   Body: { action: "start-multishot", imageUrl, prompt, promptType,
# //           duration, model }
# // GET /api/kie/video?taskId=xxx - Verificar status do vídeo Kling
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                              PREÇOS (USD)                                  │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Imagem:
#   - Nano Banana: ~$0.01/imagem
#   - Flux 2 Pro:  ~$0.03/imagem (1K), ~$0.05/imagem (2K)
#   - Grok Image:  ~$0.02/imagem
#
# Vídeo:
#   - Veo 3.1 Fast:         ~$0.10/segundo
#   - Veo 3.1 Quality:      ~$0.20/segundo
#   - Sora 2:               ~$0.15/segundo
#   - Sora 2 Pro:           ~$0.25/segundo
#   - Kling 2.5 Turbo Pro:  ~$0.12/segundo (otimizado para casais)
#   - Kling 2.5 Turbo:      ~$0.08/segundo
#
# Docs: https://docs.kie.ai
#

# =============================================================================
# ABACATEPAY - PAYMENT GATEWAY
# =============================================================================
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                    MINI TUTORIAL: CONFIGURANDO O ABACATEPAY                 │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# 1. CRIAR CONTA:
#    - Acesse: https://abacatepay.com
#    - Crie uma conta (PJ ou PF)
#    - Complete a verificação
#
# 2. OBTER API KEY:
#    - Acesse: https://app.abacatepay.com/api
#    - Crie uma nova API Key
#    - Copie a chave gerada
#
# 3. CONFIGURAR WEBHOOK:
#    - Acesse: https://app.abacatepay.com/webhooks
#    - Adicione a URL: https://seu-dominio.com/api/payment/webhook
#    - Copie o Webhook Secret gerado
#
# 4. (OPCIONAL) MODO DEV:
#    - Use o modo dev para testes sem cobrar de verdade
#    - Webhooks são disparados normalmente em dev mode
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           VARIÁVEIS DE AMBIENTE                            │
# └─────────────────────────────────────────────────────────────────────────────┘

# API Key do AbacatePay (obtida no painel)
ABACATEPAY_API_KEY=your-api-key

# Webhook Secret (gerado ao criar webhook no painel)
ABACATEPAY_WEBHOOK_SECRET=your-webhook-secret

# (Opcional) Public Key para validação HMAC de webhooks
ABACATEPAY_PUBLIC_KEY=your-public-key

# URL base da aplicação (usada para return/completion URLs)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# USO NO CÓDIGO
# =============================================================================
#
# // Importar funções do AbacatePay
# import { createBilling, PLANS } from "@/lib/abacatepay";
#
# // Criar cobrança
# const billing = await createBilling({
#   planId: "plan_1", // plan_1, plan_3, ou plan_5
#   customer: {
#     name: "João Silva",
#     email: "joao@email.com",
#   },
# });
#
# // Redirecionar para pagamento
# window.location.href = billing.url;
#
# // API Routes disponíveis:
# // POST /api/payment/create - Criar cobrança
# //   Body: { planId: "plan_1", customer: { name, email } }
# //
# // POST /api/payment/webhook - Receber notificações (usado pelo AbacatePay)
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                              PLANOS CONFIGURADOS                           │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# plan_1: 1 Geração + 1 bônus = 2 créditos   | R$9,90
# plan_3: 3 Gerações + 2 bônus = 5 créditos  | R$29,90
# plan_5: 5 Gerações + 3 bônus = 8 créditos  | R$59,90
#
# Docs: https://docs.abacatepay.com
#

# =============================================================================
# FAL.AI - VIDEO & IMAGE GENERATION
# =============================================================================
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                    MINI TUTORIAL: CONFIGURANDO O FAL.AI                     │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# 1. CRIAR CONTA:
#    - Acesse: https://fal.ai
#    - Faça login com GitHub
#
# 2. OBTER API KEY:
#    - Acesse: https://fal.ai/dashboard/keys
#    - Clique em "Create Key"
#    - Copie a chave gerada
#
# 3. ADICIONAR CRÉDITOS (se necessário):
#    - Acesse: https://fal.ai/dashboard/billing
#    - Adicione créditos conforme necessário
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                         MODELOS DISPONÍVEIS                                │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# IMAGE TO VIDEO:
#   - framepack       : Gera vídeos dinâmicos a partir de imagens
#   - minimaxVideo    : Minimax Video-01 (alta qualidade)
#   - runwayGen3      : Runway Gen3 Turbo
#   - klingVideo      : Kling Video v1
#
# TEXT TO VIDEO:
#   - animateDiff     : Fast AnimateDiff
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                           VARIÁVEL DE AMBIENTE                             │
# └─────────────────────────────────────────────────────────────────────────────┘

# API Key do fal.ai (obtida em https://fal.ai/dashboard/keys)
FAL_KEY=your-fal-api-key

# =============================================================================
# USO NO CÓDIGO
# =============================================================================
#
# // Importar funções do fal.ai
# import {
#   generateVideoFromImage,
#   submitVideoGeneration,
#   checkVideoStatus,
#   getVideoResult,
# } from "@/lib/fal";
#
# // Gerar vídeo de forma síncrona (aguarda conclusão)
# const video = await generateVideoFromImage({
#   imageUrl: "https://...",
#   prompt: "Um casal dançando sob a luz do luar",
#   aspectRatio: "16:9",
#   numFrames: 120,
# });
# console.log(video.videoUrl);
#
# // Gerar vídeo de forma assíncrona (retorna imediatamente)
# const { requestId } = await submitVideoGeneration({
#   imageUrl: "https://...",
#   prompt: "Um casal dançando",
# });
#
# // Verificar status
# const status = await checkVideoStatus(requestId);
# if (status.status === "COMPLETED") {
#   const result = await getVideoResult(requestId);
#   console.log(result.videoUrl);
# }
#
# // API Routes disponíveis:
# // POST /api/fal/video - Gerar vídeo
# //   Body: { action: "generate"|"submit"|"status"|"result",
# //           imageUrl, prompt, model?, requestId? }
# //
# // GET /api/fal/video?requestId=xxx&action=status - Verificar status
# // GET /api/fal/video?requestId=xxx&action=result - Obter resultado
#
# // Proxy para requisições diretas:
# // POST /api/fal/proxy - Proxy seguro para fal.ai
#
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │                              PREÇOS (USD)                                  │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# Video (Image-to-Video):
#   - Framepack:     ~$0.05/segundo de vídeo
#   - Minimax:       ~$0.10/segundo de vídeo
#   - Runway Gen3:   ~$0.15/segundo de vídeo
#   - Kling:         ~$0.08/segundo de vídeo
#
# Docs: https://docs.fal.ai
#
